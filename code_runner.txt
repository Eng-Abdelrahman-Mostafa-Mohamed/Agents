# from llama_index.core.tools import FunctionTool
# import subprocess


# def run_code(generated_code_from_agents):
#     try:
#         process=subprocess.run(generated_code_from_agents,shell=True,check=True,text=True,capture_output=True)
#         return process.stdout
#     except subprocess.CalledProcessError as e:
#         yield f"error accured {e.stderr}"
    
# code_runner_engine=FunctionTool.from_defaults(fn=run_code,
#                                        name='code_runner',
#                                        description='this run the generated code in terminal ',
#                                        inputs=['generated_code_from_agents'],
#                                        outputs=['output'],
#                                        )


# from llama_index.core.tools import FunctionTool
# import subprocess

# def run_code(code):
#     try:
#         process = subprocess.run(code, shell=True, check=True, text=True, capture_output=True)
#         return process.stdout
#     except subprocess.CalledProcessError as e:
#         return f"error occurred: {e.stderr}"

# # Correct usage without 'inputs' and 'outputs'
# code_runner_engine = FunctionTool.from_defaults(
#     fn=run_code,
#     name='code_runner',
#     description='this runs the generated code in the terminal'
# )
# code_sytax = """
# print('Hello World')
# """
# code = """
# python
# {code_sytax}

# """

# print(run_code(code))
